import java.util.Scanner;
import java.lang.Math;

public class ExpressionEvaluator {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
	String formula;

        formula = sc.nextLine();

	int plus = input.indexOf("+");
        int minus = input.indexOf("-");
	int four = input.indexOf("4");
	int division = input.indexOf("/");
	int asteriks = input.indexOf("*");    

	String n1 = input.substring(2,plus);
	String n2 = input.substring(input.indexOf("("+1,4),input.indexOf("*"+1,6));
	String n3 = input.substring(asteriks+1,minus);
	String n4 = input.substring(input.indexOf("*"+1,10),input.indexOf("*"+1,12));
	String n5 = input.substring(input.indexOf("*"+1,input.indexOf(")",14));
	String n6 = input.substring(input.indexOf("*",19),input.length());

	String output = (-n1+Math.sqrt(n2*n3-4*n4*n5))/(2*n6)
	System.out.println (output);

	
	
	
	
	
	
	
